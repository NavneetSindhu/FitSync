package com.minimize.maximus.ui.theme

import androidx.compose.ui.graphics.Color

// ── Dark Mode OLED Optimization ───────────────────────────────────────────
val DarkBg = Color(0xFF000000)             // Pure OLED Black
val DarkSurface = Color(0xFF111111)        // Deep gray for contrast
val DarkSurfaceVariant = Color(0xFF1C1C1E) // Neutral elevated gray
val DarkOutline = Color(0xFF2C2C2E)        // Subtle border
val DarkTextPrimary = Color(0xFFF9FAFB)
val DarkTextSecondary = Color(0xFF9CA3AF)

// ── Light Mode Depth Adjustments ──────────────────────────────────────────
val LightBg = Color(0xFFF2F2F7)            // Deeper neutral gray background
val LightSurface = Color(0xFFFFFFFF)
val LightSurfaceVariant = Color(0xFFEEF1F5)
val LightOutline = Color(0xFFD1D1D6)       // Darker border for input visibility
val LightTextPrimary = Color(0xFF111827)
val LightTextSecondary = Color(0xFF6B7280)

// ── Fitness Semantic Accents ──────────────────────────────────────────────
val AccentRed = Color(0xFFFF3B30)
val SuccessGreen = Color(0xFF22C55E)
val WarningAmber = Color(0xFFF59E0B)
val PurpleDrop = Color(0xFFA855F7)
val FailurePink = Color(0xFF991B1B)        // Deep crimson (shifted from pink)